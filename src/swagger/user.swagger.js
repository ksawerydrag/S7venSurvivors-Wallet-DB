/**
 * @swagger
 * /api/users/signup:
 *   post:
 *     summary: Create a new user
 *     description: Create a new user and add it to database.
 *     responses:
 *       201:
 *         description: Created
 */

/**
 * @swagger
 * /api/users/login:
 *   post:
 *     summary: User login
 *     description: User login to page and can use features.
 *     responses:
 *       200:
 *         description: OK
 */

/**
 * @swagger
 * /api/users/logout:
 *   get:
 *     summary: User logout
 *     description: User logout from page.
 *     responses:
 *       200:
 *         description: OK
 */

/**
 * @swagger
 * /api/users/about:
 *   get:
 *     summary: Information about the user
 *     description: User can see its username and email.
 *     responses:
 *       200:
 *         description: OK
 */
